namespace ConferenceRoomBooking.Web.Validators;

// Assembly marker used by AddValidatorsFromAssembly(typeof(ValidatorsAssemblyMarker).Assembly) —
// named differently from the containing "Validators" namespace to avoid a name/namespace clash.
public class ValidatorsAssemblyMarker;
